## Test environments

* macOS Tahoe 26.2, R 4.5.3

## R CMD check results

0 errors | 0 warnings | 2 notes

Checked locally with:

* `R CMD check --as-cran rfedqr_0.1.0.tar.gz`

Current local notes:

* New submission.
* URL check for `https://github.com/Trace231/FED-QR` timed out once during
  `R CMD check --as-cran`; the URL was verified separately and returned HTTP
  status 200.
* HTML validation skipped because the local HTML Tidy installation is not recent
  enough.

## Submission notes

This is the first CRAN submission of `rfedqr`.
